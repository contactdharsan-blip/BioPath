services:
  # Combined Backend + Frontend
  - type: web
    name: biopath
    runtime: docker
    dockerfilePath: ./Dockerfile.render
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
